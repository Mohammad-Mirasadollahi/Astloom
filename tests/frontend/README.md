# Frontend Tests

Frontend tests belong here.

Expected future layout:

```text
tests/frontend/<app-or-feature>/
  *.spec.ts
  *.spec.tsx
  test_*.ts
  test_*.tsx
```

Document the exact frontend test command here when the frontend package and runner are introduced.
