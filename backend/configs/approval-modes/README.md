# Approval mode profiles

Versioned `ApprovalModeProfile` JSON for GAP-004.

- Schema: `approval-mode-profile.schema.json`
- Default catalog entry: `default.json` (`mode=manual`)
- Runtime override: project state `approval_mode` / env `ASTLOOM_APPROVAL_MODE`
- First Accept surface: Astloom CLI (`astloom approval …`)
