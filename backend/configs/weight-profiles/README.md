# Weight profiles

Versioned `WeightProfile` catalog for GAP-006 governance.

- Schema: `weight-profile.schema.json`
- Catalog entries: `*-memory-profile.json`
- Activation / rollback history: `.astloom/weight-profile-governance.json` (via CLI)
- Change control: Memory Platform Lead (or delegate) must set `approved_by` before activate
