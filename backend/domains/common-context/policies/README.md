# Common Context Policies

Defines future promotion, deduplication, injection, override, isolation, review, expiry, and retirement policies. Score weights and thresholds must be configuration-driven, not hard-coded.
