# Common Context Model

Defines future concepts such as CommonItem, CommonContextScope, ApplicabilityRule, ReuseScore, ConflictPolicy, and ContextBundle. Each model must be project-scoped, versioned, audit-friendly, and free from hidden global state.
