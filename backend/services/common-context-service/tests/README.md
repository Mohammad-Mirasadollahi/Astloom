# Common Context Service Tests

Future tests must cover lifecycle, proposal scoring, scope isolation, conflict resolution, token budget trimming, bundle explanation, audit evidence, and cases where common context must not be injected.
