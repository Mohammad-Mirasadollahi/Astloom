# Common Context Service Source

Future code should separate application use cases, domain model, infrastructure adapters, API handlers, workers, and bootstrap modules. Bundle resolution must be deterministic and testable.
