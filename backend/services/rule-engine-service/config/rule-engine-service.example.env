# PostgreSQL is the only runtime persistence backend.
ASTLOOM_RULE_ENGINE_DATABASE_URL=postgresql://astloom:change-me@postgres:5432/astloom
# Judge adapter: heuristic (default, tests/bootstrap) or litellm (GAP-T05).
ASTLOOM_RULE_JUDGE=heuristic
