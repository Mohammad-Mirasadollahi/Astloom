# Outbox Relay Package

Path: `backend/packages/outbox_relay`

## Purpose

Shared library for polling service outboxes, running relay handlers, and marking events published.

## Status

Implemented. Used by `backend/apps/worker` (`python -m astloom_worker`).
